#!/usr/bin/env bash
set -euo pipefail

# Optional hook: customize after the project stack is known.
# It intentionally performs no action by default, so the reusable template
# never guesses npm/pnpm/python/rust commands or mutates a new repository.

exit 0
